<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Create a planet</title>
  </head>
  <body>

    <h1>Edit <%= planet.name %></h1>
    <form action="/planets/<%= planet._id %>?_method=PUT" method="POST">
      <label for="name">Name:</label>
      <input type="text" name="name" id="name" value="<%= planet.name %>"/>

      <div class="checkbox-div">
        <label for="ready-to-see">Ready to See?</label>
        <input type="checkbox" name="isReadyToSee" id="ready-to-see" <% if (planet.isReadyToEat) { %> checked <% } %> />
      </div>
        
      <button type="submit">Update planet</button>
    </form>

    <a class="planets-index-link" href="/planets">
      <div class="index-link-text">Back to planets</div>
    </a>
  </body>
</html>